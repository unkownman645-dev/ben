{
  "name": "clinical-intake-prototype",
  "version": "1.0.0",
  "description": "Clinical intake and doctor triage prototype",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
